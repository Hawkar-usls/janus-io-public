# Undina's Comb bridge into A18.43

A18.42 is not discarded. It is the control organ used by A18.43.

```text
A18.40  sees the post-target cohort-drain tail
A18.41  measures timing and shows when external stopping is too late
A18.42  creates native OPEN → HOLD → DRAIN → clean valley → REOPEN epochs
A18.43  asks whether that control removes unnecessary work, joules, and heat
```

## Frozen meanings

- **Wave** — an admitted epoch/cohort.
- **Crest** — completions belonging to new admissions after a validated clean valley.
- **Valley** — admissions closed and old in-flight work drained to zero.
- **Foam** — old in-flight completions after reopen; never a new crest.
- **Tail** — work completing after the useful target or stop boundary.
- **Undina's Comb** — the native pre-dispatch gate that shapes those epochs.

The preservation challenge does not replace wave research with temperature monitoring. It joins them:

```text
wave accounting + exact exposure + hardware telemetry
→ useful work / all work
→ CPU-package joules per useful batch
→ thermal stress per useful batch
```

A18.42 is frozen as `REPLICATED_NATIVE_WAVE_SEGMENTATION`, not as a proven useful Wave Pump. A18.43 must determine whether the sealed gate behavior produces a measurable preservation benefit.

V0.1.3 validates 15 deterministic wave-semantics faults in addition to 13 hardware-evidence faults. Synthetic traces validate the analyzer only; the future live matched run remains the only source of physical preservation evidence.
