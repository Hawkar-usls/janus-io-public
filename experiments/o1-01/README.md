# O1-01

This directory contains curated O1-01 analysis outputs and public-facing
experiment notes. The original raw evidence tree remains at `janus_io_o1_runs/`
so existing script-relative paths and proof archives are not disturbed.

Run:

```powershell
python scripts\analyze_o1.py
```

The analyzer writes `o1_analysis_summary.json` and `o1_analysis_summary.md`
here.
