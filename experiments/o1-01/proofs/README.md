# Proofs

Raw accepted-share proof archives already exist under
`janus_io_o1_runs/<agent>/proofs/`. They are intentionally not moved during
repository cleanup because the legacy supervisor and reports use script-relative
paths.

Curated or redacted proof bundles for publication can be placed here after
review.
