# A10 PY314 / Windows Terminal Fix - 2026-06-13

This note records a public-safe A10 line maintenance checkpoint. It describes
runner compatibility and launcher restoration only. It is not a raw live log,
not a proof archive, and not an operational instruction.

## Scope

The A10 runner was made compatible with Python 3.14 by fixing argparse
percent/help-string compatibility. The affected surface is command-line help
formatting and parser compatibility, not mining behavior.

The Windows Terminal / PowerShell launcher path was also restored for the
PY314 runner path. The launcher restoration keeps the public entrypoint
auditable while preserving the live-run acknowledgement boundary used by the
public snapshot.

## Frozen Boundary

The maintenance checkpoint preserves these boundaries:

```text
WIRE: frozen
HASH: frozen
SUBMIT: frozen
randomized traversal mirror: untouched
Back/Forth / Secret / glyph ecology: observer-only or shadow-only
```

No operational behavior was intentionally changed:

- no hash-loop change;
- no nonce traversal change;
- no submit-policy change;
- no Stratum wire change;
- no randomized traversal mirror change;
- no proof acceptance rule change;
- no live scheduler behavior change.

## Claim Boundary

This checkpoint makes no cryptanalytic or profit claim:

- no SHA-256 break claim;
- no nonce prediction claim;
- no mining profit claim;
- no claim that glyph observations decode a confirmed message.

The correct status remains:

```text
controlled PoW traversal benchmark
accepted-share rare-tail telemetry
observer-only sidecar
frozen wire boundary
semantic unconfirmed
```

## Public-Safe Interpretation

The PY314 and Windows Terminal work improves reproducibility and local operator
recovery. It does not make JANUS stronger by touching WIRE / HASH / SUBMIT.
Any Back/Forth, Secret, or glyph ecology output remains a shadow or observer
layer: useful for documentation and later review, but not a scheduler rule.
